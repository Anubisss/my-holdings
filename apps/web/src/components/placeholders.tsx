/** Em-dash placeholder shown when a value is unavailable. */
export const dash = <span className="text-slate-400 dark:text-slate-500">—</span>;

/** Note shown when a holding has no FX rate available. */
export const fxNote = (
  <span className="text-xs text-amber-600 dark:text-amber-400">No FX rate</span>
);
